// Verifica se a página foi carregada completamente antes de executar o modal
$(function(){
    var idModal = '#sendMailModal';

    $('#recipient-name').val('<%= @admin.email %>');
    $('#exampleModalLabel').text('Enviar mensagem para <%= @admin.name %>');
    $(idModal).modal();
    $(idModal).on('shown.bs.modal', function () {
      $('#message-text').focus();
    })
});
